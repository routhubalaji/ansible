- name: nginx install
  host: frontend
  tasks:
  - name: install nginx
    ansible.builtin.service:
      name: nginx
      state: present
  - name: start nginx
    ansible.builtin.service:
      name: httpd
      state: started 
      enabled: yes