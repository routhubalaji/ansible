- name: Checking variables scope
  hosts: local
  connection: local
  vars:
    NAME: From Playlevel
  # vars_files:
  #   - course.yaml
  # vars_prompt:
  #   - name: NAME
  #     prompt: "What is your name?"   
  #     private: false
  tasks:  
  - name: Testing task level vars
    # vars: 
    #   NAME: From task level

    ansible.builtin.debug:
      msg: "Name is: {{NAME}} "  